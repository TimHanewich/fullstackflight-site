Never commit anything to git unless you absolutely have to. The user prefers to review your updates via a `git diff` and then commit himself.

When extending the site or adding content, preserve look and feel where appropriate.